package com.molten.optimization.tiled

import com.molten.optimization.command.RenderCommand

class TileCuller {
    fun cull(commands: List<RenderCommand>, tileX: Int, tileY: Int, tileWidth: Int, tileHeight: Int): List<RenderCommand> {
        return commands
    }
}
