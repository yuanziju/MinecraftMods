package com.molten.optimization.resource

import com.molten.optimization.MoltenMod

object TextureCompressor {
    fun compress(image: Any): Any {
        MoltenMod.LOGGER.debug("Compressing texture")
        return image
    }
}
